Do not change any code without explanation or inline comments. 
Give me full typed files based on my existing structure and recent code/file modifications not placeholders and inline 
comments on the exact changed lines/code.
Also make sure codes are mobile responsive and responsive to darkmode theme.

'ARDMS' | 'Sonography Canada' | 'CAMRT' | 'ARRT' | 'CCI'

One last thing to watch
Make sure any old UI or shared helper still doesn’t do this:

category === 'CPD'
category !== 'CPD'
Those should be fully removed now.

If you want, I can do one more pass and point out the remaining files likely still using old category-based mode logic.

If you want, I can now help you find any remaining references to submitQuizAttempt or /api/attempts/submit so you can remove them cleanly.


Repo: Captainzik/radpreps
Branch: staging
File path: app/quiz/page.tsx



If you want, I can next give you:

the updated lib/actions/user.actions.ts to wire wallet defaults into new users,
the reward service actions for hearts/gems/XP, or


Important note
These audio files reference paths like:

/audio/correct.mp3
/audio/wrong.mp3
So make sure your public/audio/ folder contains those files.




//whole plan
Before you continue i want to reveal all my plans for this application, 
i want to mention that I will be needing either hearts or battery status like
in duolingo, and when user runs out of hearts or battery he can earn more by watching ads, also gems, 
timed XP boosts e.g doing a lesson between 6am to 12pm. Quests[friend quest/Friend challenges, weekly quest]
I will need to show ads and reward them with gems which they swap or use to get hearts, when they refer a 
friend they earn gems which they can trade for hearts, they can send gems to friend as well.
Any user that does not want to watch ads will need to upgrade to premium. 
In Exam mode ads will be served after every 10 questions(i.e at checkpointIndex) and most questions length is about 100 or more. 
In CPD mode only 10 questions will be given per quiz and then ads at end(checkpointIndex), or ads if user ran out of hearts while
attempting the quiz.
Because questions can be as long as 200 or more I need CPD quiz to break the questions in 10s, grade every checkpoint index and serve 
another quiz for next 10